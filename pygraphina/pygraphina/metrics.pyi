"""Graph metrics namespace.

The metric functions are exposed as methods on PyGraph and PyDiGraph (for
example, ``graph.diameter()``, ``graph.radius()``, ``graph.transitivity()``,
and ``graph.average_clustering()``). This module is a namespace placeholder
reserved for future standalone functions.
"""
